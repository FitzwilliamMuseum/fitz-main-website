<?php

namespace Tests\Feature\Models;

use Tests\TestCase;

class TessituraPerformancesTest extends TestCase
{
    public function test_get_exhibition_performances_returns_mixed()
    {
        $this->markTestSkipped('No access to API endpoint for TessituraPerformances.');
        // $id = 'test-id';
        // $result = \App\Models\TessituraPerformances::getExhibitionPerformances($id);
        // // Since this returns mixed type and depends on external API, we just test it doesn't throw an error
        // $this->assertTrue(true);
    }
}
